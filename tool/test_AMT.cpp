#include <iostream>
#include <cassert>
#include <cassert>
#include "utils.h"
#include "AMTrieNode.h"
#include <bitset>

using namespace std;

int main(int argc, const char **argv) {
	assert(argc=2);


   /* cout << _count << endl;*/
	//const char *fname = argv[1];
	//uint8_t *buf;
	//uint64_t bufSize;
	//bool result = loadfile(fname, buf, bufSize);
	//assert(result);

	//vector<string> tokens;
	//splitString((const char *)buf, bufSize, '\n', tokens);

	//assert(tokens.size() > 10);

	//AMTrieNode *t = new AMTrieNode;

	//for (int ti=0; ti<tokens.size(); ++ti) {
		//string token = tokens[ti];
		//t->addEntry(token.c_str(), token.size());
	//}
	
	//bitset<256> map;
	//cout << sizeof(map) << endl;
	//cout << _count << endl;
	//int dummy;
	//cin >> dummy;

	//t->compact();
	//cin >> dummy;
	/*return 0;*/
}
